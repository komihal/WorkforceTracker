<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Дополнительные разрешения для работы в фоновом режиме -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Network permissions for background operation -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!-- Additional permissions for background operation when phone is locked -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- For Android 13+ photo picking/saving -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- For Android 12- (granted at runtime); kept for backward compatibility -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Explicitly declare CAMERA to align with runtime request flow -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- For motion detection -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
      android:name=".MainApplication"
      android:enableOnBackInvokedCallback="true"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:hardwareAccelerated="true"
      android:largeHeap="true"
      android:persistent="true"
      android:networkSecurityConfig="@xml/network_security_config"
      android:usesCleartextTraffic="true"
      android:requestLegacyExternalStorage="true"
      android:extractNativeLibs="true">
      <meta-data
        android:name="android.app.shortcuts"
        android:resource="@xml/shortcuts" />
      <!-- Transistorsoft Background Geolocation license (for native validation on Android) -->
      <meta-data android:name="com.transistorsoft.locationmanager.license" android:value="7d1976aa376fbcf7e40d12892c8dab579985abbcbc09e1da570826649b4295cf"/>
      <!-- Force smallIcon for Foreground Service notification at manifest-level -->

      <meta-data android:name="com.transistorsoft.locationmanager.notification.LargeIcon"
                 android:resource="@drawable/ic_action_icon"/>
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/ic_stat_notify" />
      <!-- OPPO/OnePlus specific metadata -->
      <meta-data android:name="RegisterAgentType" android:value="1"/>
      <meta-data android:name="AccessoryServicesLocation" android:value="1"/>
      <meta-data android:name="oplus.view.mirror.enable" android:value="false"/>
      <meta-data android:name="oplus.view.mirror.support" android:value="false"/>
      
      <!-- Anti-freeze metadata -->
      <meta-data android:name="android.app.background_restricted" android:value="false"/>
      <meta-data android:name="android.app.allow_backup" android:value="true"/>
      <meta-data android:name="android.app.allow_clear_user_data" android:value="false"/>
      
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTop"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <activity
        android:name=".SelfieActivity"
        android:exported="false"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.AppCompat.NoActionBar" />
      
      <!-- BackgroundGeolocation Foreground Service -->
      <service android:name="com.transistorsoft.locationmanager.LocationManagerService"
               android:enabled="true"
               android:exported="false"
               android:foregroundServiceType="location" />
               
      <!-- Background Service for data upload -->
      <service android:name="com.workforcetracker.BackgroundUploadService"
               android:enabled="true"
               android:exported="false"
               android:foregroundServiceType="dataSync" />
    </application>
</manifest>
